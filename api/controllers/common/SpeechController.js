/**
 * Created by TuanNT22 on 17-07-2015.
 */
/*
var tts = require('node-google-text-to-speech')
module.exports = {
  tts: function (req, res) {
    var tl = req.param('tl');
    var w = req.param('w');
    tts.translate(tl, w, function (result) {
      if (result.success) { //check for success
        res.send({'audio': result.audio});
      }
    });
  }
}
*/
