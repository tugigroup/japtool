
<div class="col-sm-12 content"><h1>Infomation user <%- user.username %></h1></div>
<div class="col-sm-6">
    <% if(user.admin){ %>
    <td><img src="/images/admin.png"/> Admin</td>
    <% } else{ %>
    <td><img src="/images/pawn.png"/>Member</td>
    <% } %>

    <p><%- user.firstname %> <%- user.lastname %></p>
    <p>Email: <%- user.email %></p>
    <p>Address: <%- user.address %></p>
    <p>Website: <%- user.website %></p>

    <a class="btn btn-danger btn-group-lg" href="/user/edit/<%= user.id %>">Edit</a>
</div>
<div class="col-sm-6">
    <div class="row">
        <div class="col-sm-4">
            Search Friend:
        </div>
        <div class="col-sm-8">
            <form action="/user/searchUser" method="POST" class="form-inline">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Search Friend" name="searchUser">
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Search Friend</button>
                </div>
            </form>
        </div>
    </div>

</div>